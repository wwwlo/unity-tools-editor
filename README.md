SnapToCollisionEditor - притягивает дочерние объекты к поверхностям коллизий по вертикали

Remove bad prefab     - prefabs без исходного ассета (PrefabAssetType.MissingAsset)

LODSizeFixer          - автоматически исправляет слишком маленькие LOD-группы в префабах

ReplaceObjectsWindow  - позволяет заменить выбранные объекты на другой Prefab (префаб)

RenderTextureBaker    - скрипт для выпечки (baking) текстур из камеры или RenderTexture в PNG файлы

VertexPaintWindow_v3  - позволяющее рисовать по вертексам мешей без изменений оригинала

FillObject            - автоматического распределения объектов-предметов по поверхности с учетом плотности, угловых ограничений и опционального масштабирования через текстуру

MeshGridSpawnerWindow - создаёт упорядоченную сетку из моделей (предметов)

ObjectBySpline        - размещение объектов непосредственно на сплайне или в области вокруг него

ObjectPainterWindow_v2- позволяет пользователю размещать (посевать) объекты-префабы на поверхности сцены, а также управлять уже созданными объектами Режим рисования (Painting): Режим ластика (Eraser): Масштабирование (Scaling): Притягивание ( Attraction)

PCacheToNatureRenderer- которое конвертирует файлы point cache из Blender (.pCache) в объекты NatureInstance из плагина VisualDesignCafe

PhysicsObjectPainter  - позволяет симулировать физику для выбранных объектов с LOD‑группами и применять результаты обратно в сцене

VertexSpawner         - нструмент для спавна префабов на вершинах меша целевого объекта

ReplaceObjectsPrefab  - замены всех объектов с одинаковым именем на префаб 

ExportGrassInstances  - позволяет экспортировать данные растительных экземпляров (grass instances) из Terrain в отдельные файлы .asset

SplatmapEditor        - Позволяет рисовать прямо на текстурах материалов с поддержкой UV-каналов, режимов смешивания и автоматического сохранения. При нескольких материалах — выбор в выпадающем списке (индекс 0 по умолчанию) Поддерживает два типа splatmap: _SplatMap или _SplatMap2

SplatmapInstancePlacer- 
